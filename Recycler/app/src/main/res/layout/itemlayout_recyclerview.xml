<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imgv_dpic"
        android:layout_width="120dp"
        android:layout_height="120dp"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/txtv_dname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txtv_dseats"
        android:layout_marginLeft="8dp"
        android:layout_marginBottom="3dp"
        android:layout_toRightOf="@id/imgv_dpic"
        android:text="Dept name"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/txtv_dseats"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/imgv_dpic"
        android:layout_marginLeft="13dp"
        android:layout_marginBottom="18dp"
        android:layout_toRightOf="@id/imgv_dpic"
        android:text="seats"
        android:textSize="24sp" />
</RelativeLayout>